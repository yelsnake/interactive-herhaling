const listOfNumbers = [2, 3, 5, 7, 11];
// voor elk element van de array ListOfNumbers
for (let entry of listOfNumbers) {
    console.log(entry);
}
console.log(listOfNumbers);
listOfNumbers.forEach((entry) => {console.log(entry);});
